{
  "name": "example",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "@twilio/runtime-handler": "1.2.0",
    "express": "^4.17.1",
    "twilio": "^3.70.0"
  },
  "devDependencies": {
    "twilio-run": "^3.3.0"
  },
  "engines": {
    "node": "12"
  }
}
